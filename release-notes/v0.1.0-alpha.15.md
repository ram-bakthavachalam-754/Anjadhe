# Anjadhe v0.1.0-alpha.15

## Fixed

- Built-in web search recovers on its own if the service stops
  recognizing this Mac's access key (it now discards the stale key, gets
  a fresh one, and retries — previously every search failed with
  "Unknown API key" until the key was refreshed from Settings). Monthly
  usage is unaffected by the refresh.
